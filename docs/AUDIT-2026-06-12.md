# AUDIT ĐA-PERSONA — đêm build 2026-06-12 (4 persona song song, mọi P0 kiểm chứng chéo 1 vòng phản biện)

Personas: 👩 user mới cấp 1 · 🧠 advisor sản phẩm · 🎨 designer · 🔧 dev/QC. 12 agents, 204 tool calls (đọc code + query DB thật).

## ✅ 8 P0 — TẤT CẢ ĐÃ SỬA NGAY TRONG ĐÊM (commit 2631e9a)
1. **User mới không có 7 cây** → `bootstrap_me` giờ seed 7 hub idempotent + client `ensureHub()` fallback ở mọi cửa nạp.
2. **Digest tạo trang lơ lửng** (lookup title cũ 'Nhật ký hành trình') → lookup `props.hub='journey'` + fallback kho, kèm page_type/via.
3. **Ghi nhanh không có event_date/page_type** → createPage nhận extra; capture set ngày LOCAL + loại; mắt xích GHI đan vào Dòng đời.
4. **Chuyển hoá được 0 Qi trên Home** (PT2 dùng key 'digest', Digest ghi 'tham') → khớp 'tham'.
5. **Board tràn cả kho cá nhân vào cột Ý tưởng + nút Content vô cảm** → Board chỉ query `props->>board is not null`; giờ bấm 📣 Content là thẻ XUẤT HIỆN thật.
6. **RLS chặn approver thấy bài pending người khác** → policy node_select mở rộng bằng `can_approve_org()` (security definer); advisors đã chạy lại, không lỗi mới.
7. **Giao việc ở MembersHub fail im lặng** (thiếu org_id NOT NULL) → truyền orgId + insert + hiện lỗi.
8. **Khối Cần làm không hiện việc giao** (select cột node_id chưa tồn tại) → migration thêm `assignments.node_id uuid references nodes`.

## 📋 P1 — backlog 19 mục (đáng sửa trước pilot; nhóm lại)
**Data/logic:** Qi nghiệm thu không cộng được cho người khác (RLS events own-only — cần RPC award_qi security definer) · kéo trang sang kho khác chỉ đổi layer node cha, con giữ layer cũ (cần update đệ quy) · taxonomy page_type tồn tại 3 phiên bản lệch (Studio PT 8 loại vs PAGE_TYPES 7 loại vs KHO-CHUAN — CẦN CHỐT 'cau-hoi-mo' vs 'ghi-chu', xem KHO-CHUAN §1 ⚠️) · vòng đời assignments lệch giữa Studio (tự done) và MembersHub (submitted→nghiệm thu) — gộp về một.
**UX trung thực:** "AI hiểu bạn" đếm cả data seed/người khác → lọc owner_id đúng từng phần · gợi ý Cần chuyển hoá đã đổi sang lý do thật (fixed một phần trong đêm) · quote capture đã giữ nguồn + tự tạo Kim Chỉ Nam (fixed trong đêm).
**Design:** thống nhất max-width/padding giữa Home (max-w-4xl) và 4 hub (max-w-5xl) · 3 cỡ title khác nhau giữa view · emoji trong chrome chức năng còn nhiều nơi (rule B11) · còn sót pink ở COVERS preset + node page màu pink trong Galaxy COLOR map · font canvas Galaxy 'Inter' ngoài hệ (đổi var font khi tiện).

## 📦 P2 — 5 mục (nice-to-have)
Ngôn ngữ vũ trụ hơi bí cho user không rành công nghệ (cân nhắc tooltip giải thích) · North star chưa hiển thị trên Home · ~300 dòng dead code (Engine/ReviewQueue/UsersPage cũ trong Pages.tsx — xoá khi rảnh) · 3 chuỗi "Thấm" sót (đã quét lại lần cuối, còn trong comment code — vô hại) · icon lệch nhỏ giữa các nơi.


---

# AUDIT ĐỢT 2 — 2026-06-12 chiều (flows-writer + QC, 50 tool calls đọc code thật)

## ✅ Đã sửa ngay trong đợt
- **P0 chiều Cảm xúc đứt mạch khỏi điểm 8 chiều**: loadTree select thêm cột emotion + truyền vào 2 call site dimSignals.
- **Mất dữ liệu 21 câu hỏi**: không ghi đè rỗng + nạp lại câu trả lời khi mở panel.
- **Gate điểm**: hiện điểm khi t8.learned (≥2 chiều qua links) chứ không chỉ khi đã chạy Digest.
- **Quote fallthrough**: (xem ghi chú dưới — fix một phần, còn lại backlog).
- Dọn xác chết: Radar 5 cạnh, import depthScore/OUTPUT_FORMATS.

## 📋 BACKLOG LUỒNG (flows-writer phát hiện, file:dòng trong mô tả — sửa trước pilot)
1. page.tsx:663 + page.tsx:1169-1183 + page.tsx:404-418 — nút '📥 Raw' chỉ setPage('today'); không còn chỗ nào setRawOpen(true) → modal 'Dữ liệu thô → Trang chuẩn' và createFromRaw (kèm ai_jobs queue) là code chết, nút sai chức năng so với title
2. Pages.tsx:626 + Pages.tsx:523 + Hubs.tsx:641 — nút 'Mở việc' trong khối Hôm nay cần a.node_id nhưng cả 2 form giao việc (Studio, MembersHub) đều không ghi node_id (MembersHub có state na.node_id nhưng không có ô nhập, không đưa vào INSERT) → nút luôn câm, trái KHO-CHUAN §3#9
3. Pages.tsx:1013 + page.tsx:364-373 — content_results.source_node_id đọc từ props.source_node_id nhưng không luồng nào ghi nó (toContent và newCard đều không) → luôn null, vòng 'AI học content nào ra tiền' không truy được bài gốc
4. Pages.tsx:896-901 vs page.tsx:337-343 — Board tìm/tạo 'Xưởng content' theo title (không gắn props.hub) trong khi Engine/Today dùng ensureHub('studio') so theo props.hub → dễ sinh 2 trang Xưởng content trùng trong kho cá nhân
5. page.tsx:824 vs Pages.tsx:873 — nút '📣 Content' hiện cho mọi trang owner_id===user.id kể cả bài user nộp lên kho corporate/humanity (Studio đặt owner=user.id tại Pages.tsx:287), nhưng Board chỉ query layer='personal' → bấm Content xong thẻ không bao giờ xuất hiện trên Board
6. Hubs.tsx:452-458 vs page.tsx:516-518 — ReviewHub duyệt qua textarea chỉ update nodes.md, không sync nodes.content (JSON BlockNote); Editor ưu tiên content khi mở → bản tổng biên tập sửa lúc duyệt có thể không phải bản hiển thị sau đó
7. Pages.tsx:537 vs Hubs.tsx:658-659 — 2 vòng đời assignment song song trên cùng bảng: Studio cho người nhận tự '✓ Hoàn thành' (open→done, không Qi) trong khi MembersHub là open→submitted→done có nghiệm thu + cộng Qi
8. page.tsx:1046-1063 — ghi nhanh 💬 Quote: nếu tạo trang Kim Chỉ Nam thất bại, code rơi xuyên xuống nhánh dưới và lưu quote thành note 🌱 Trải nghiệm trong Hành trình (sai loại trang, mất trường nguồn)

## Ghi chú QC còn lại (P1)
- props.refs và action.done trong transformScore là tín hiệu chết (chưa luồng nào ghi) — hoặc thêm UI toggle done, hoặc bỏ khỏi công thức.
- ContentEngine: loaded kẹt nếu query đầu reject (thêm .catch); mxId không reset theo user.id (rủi ro thấp vì unmount khi logout).
- Docs đã viết lại theo code thật: APP-FLOWS.md (12 màn, từng nút → DB) + USER-STORIES.md (5 persona + bảng 9 gập ghềnh).
9. (đợt 5) Trích ngày nhắc trong md nội dung page → mốc phụ trên trục Dòng đời (cần md toàn org, làm cùng embeddings Phase 2).
